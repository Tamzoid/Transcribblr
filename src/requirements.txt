cutlet
fugashi[unidic-lite]
demucs
pydub
pytest
pytest-cov
# Context generation (Step 5a — LLM-based entity extraction + summarisation).
# Heavy: ~5GB VRAM, requires CUDA. Skipped at install time if torch isn't pulled
# in already; loaded lazily on first /generate-context call.
transformers==4.46.0
peft
accelerate
bitsandbytes
# Transcription — pin WhisperX <3.8. Newer versions (3.8+) require
# transformers>=4.48 and numpy>=2.1, which conflict with Colab's preinstalled
# numpy 2.0.x and would force a kernel restart to swap in. Older WhisperX
# accepts the existing transformers 4.46 + numpy 2.0 stack as-is.
whisperx<3.8
