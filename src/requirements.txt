cutlet
fugashi[unidic-lite]
demucs
pydub
pytest
pytest-cov
# Lightweight HTTP-based translation for /context-edit (single character /
# vocab / description edits). Avoids loading C3TR for tiny strings — that
# was contending with Whisper/Qwen for VRAM and triggering meta-tensor
# errors. C3TR is still used for the heavy /generate-context pipeline.
deep-translator
# Context generation (C3TR-Adapter) — pinning transformers to 4.46.0 because
# 4.48+ pulls dependencies that need a newer numpy than Colab pre-installs,
# which breaks `import transformers` at runtime with `_center` missing.
# huggingface_hub 0.26.x is what 4.46.0's internals expect — newer hub
# versions rename/remove symbols 4.46 imports (per Step 5b's notes).
huggingface_hub==0.26.5
transformers==4.46.0
peft
accelerate
bitsandbytes
# Transcription — plain openai-whisper. Avoids whisperx's eager import of
# transformers.models.wav2vec2 (for forced-alignment), which was reaching
# into numpy private symbols (_center) that Colab's pre-installed numpy
# 2.0.x doesn't expose. We keep multi-pass behavior by varying audio
# padding per pass; VAD is already handled upstream by /process.
openai-whisper
