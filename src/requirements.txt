cutlet
fugashi[unidic-lite]
demucs
pydub
pytest
pytest-cov
# Context generation (Step 5a — LLM-based entity extraction + summarisation).
# Heavy: ~5GB VRAM, requires CUDA. Skipped at install time if torch isn't pulled
# in already; loaded lazily on first /generate-context call.
# transformers 4.48.0 is the lowest version WhisperX 3.8.5+ accepts and is also
# known-good with C3TR-Adapter (Gemma-2-9B + PEFT) — confirmed by the upstream
# Step 5 Colab notebook.
transformers==4.48.0
peft
accelerate
bitsandbytes
# Transcription (Step 5 — multi-pass WhisperX). Heavy: ~3GB VRAM, requires CUDA.
# Loaded lazily on first /transcribe call.
whisperx @ git+https://github.com/m-bain/whisperX.git
